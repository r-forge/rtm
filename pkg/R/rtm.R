rtm <- function(corpus,
                K,
                alpha = 0.1,
                eta = 0.1,
                beta = 3,
                num.iterations = 25) {
  beta <- rep(beta, length.out = K)  
}
